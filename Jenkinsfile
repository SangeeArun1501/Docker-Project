pipeline {
    agent any
    stages {
        stage('Check Jenkins Path') {
            steps {
                script {
                    sh 'echo $PATH'
                }
            }
        }
    }
}
